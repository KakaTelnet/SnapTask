# Snap Goal Make Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `snap-goal-make`, a general multi-turn AI Agent Goal generator that reads authorized software context, produces a fixed-shape Goal, and hands the confirmed Goal to `snap-goal-review` as the sole quality judge.

**Architecture:** Implement the Maker as a conversational protocol plus a canonical Goal template. Keep quality scoring exclusively in `snap-goal-review`; Maker consumes its existing verdict and findings, regenerates a complete Goal after revisions, and hands an unchanged approved Goal to `task-decompose`. Validate the behavior with persistent JSON cases, a fresh-context Codex harness, official Skill and plugin validators, and an isolated plugin installation.

**Tech Stack:** Markdown Codex Skills, YAML agent metadata, JSON plugin and evaluation fixtures, Python 3.13 standard library validation harnesses, Codex CLI.

## Global Constraints

- Work in `/Users/kaka/kDevelop/Skills/SnapTask`.
- Preserve untracked `skills/snap-goal-review/skill_cn.md`, `skills/snap-goal-review/references/review-rubric_cn.md`, and `tests/goal-fixtures/`; do not stage or modify them.
- Maker does not score, apply the Reviewer's eight hard gates, or emit `通过: Approved`, `修订: Revision Required`, or `驳回: Rejected` as its own judgment.
- `snap-goal-review` remains the sole quality judge and keeps its existing output fields.
- Do not add patch-based revision, Goal version numbers, persistent workflow state, or programmatic JSON review returns.
- Revision uses targeted questions followed by a complete regenerated Goal and user confirmation.
- Use a fixed Goal skeleton with flexible length; explain any non-applicable field instead of silently omitting it.
- Use `apply_patch` for manual edits.
- Before every Python execution, activate the project `venv`, verify `which python3` and `which pip3` point inside it, then run the command. Create `venv` from pyenv Python 3.13.9 if it does not exist; never commit `venv/`.
- Temporary validation scripts belong in `tmp_py/`; durable evaluation data belongs in `tests/`.

---

## File Map

**Create**

- `skills/snap-goal-make/SKILL.md`: conversational modes, context policy, drafting rules, and handoff behavior.
- `skills/snap-goal-make/references/goal-template.md`: canonical result and bounded-investigation Goal forms.
- `skills/snap-goal-make/agents/openai.yaml`: launcher metadata and default prompt.
- `tests/goal-make-cases.json`: fourteen durable behavior cases.
- `tmp_py/tmp_20260715_goal_make_contract.py`: static contract and fixture coverage validator.
- `tmp_py/tmp_20260715_run_goal_make_evals.py`: fresh-context Codex behavior harness.

**Modify**

- `skills/snap-goal-review/SKILL.md`: replace the obsolete `$snap-goal` revision destination with `$snap-goal-make`.
- `skills/task-decompose/SKILL.md`: send Revision Required Goals back to `$snap-goal-make`.
- `.codex-plugin/plugin.json`: advertise Goal creation, review, and decomposition; bump the minor version.
- `AGENTS.md`: document the third Skill and its validation commands.

**Read but do not modify**

- `skills/snap-goal-review/references/review-rubric.md`: Reviewer-owned quality policy.
- `skills/task-decompose/SKILL.md`: approved and bypass handoff behavior.
- `tests/goal-review-cases.json`: existing Reviewer coverage.

---

### Task 1: Add the Goal Maker contract, evaluation fixtures, and core Skill

**Files:**
- Create: `tests/goal-make-cases.json`
- Create: `tmp_py/tmp_20260715_goal_make_contract.py`
- Create: `skills/snap-goal-make/SKILL.md`
- Create: `skills/snap-goal-make/references/goal-template.md`

**Interfaces:**
- Consumes: rough intent, an existing Goal draft, or a `snap-goal-review` report; authorized local files and user-provided sources.
- Produces: Discovery summary, complete Goal draft, review handoff, complete revised Goal, or approved handoff guidance.
- Delegates quality judgment to: `$snap-goal-review`.
- Delegates approved decomposition to: `$task-decompose`.

- [ ] **Step 1: Create the fourteen behavior cases**

Create `tests/goal-make-cases.json` with this exact shape and content:

```json
{
  "schema_version": 1,
  "cases": [
    {
      "id": "discovery-vague-outcome-zh",
      "mode": "discovery",
      "language": "zh",
      "input": "我想让登录功能更好。现在开始帮我生成 Goal。",
      "must_include": ["已确认", "暂定假设", "待确认"],
      "must_exclude": ["审核结果:", "综合评分:", "通过: Approved"]
    },
    {
      "id": "context-aware-software-zh",
      "mode": "discovery",
      "language": "zh",
      "input": "为当前 SnapTask 项目新增 Goal 创建能力。先读取当前项目中被授权读取的 AGENTS.md、插件清单和相关 Skill，再继续访谈。",
      "must_include": ["AGENTS.md", "已确认", "待确认"],
      "must_exclude": ["无法访问任何本地文件", "综合评分:"]
    },
    {
      "id": "bounded-investigation-draft-zh",
      "mode": "draft",
      "language": "zh",
      "input": "生成完整 Goal 草稿：在 4 小时内只读检查 auth-api/error.log、gateway/access.log 和最近 20 个认证相关提交，交付已验证事实、排除的假设、最可能原因、未知项和下一步；不保证找到根因；禁止修改代码、配置和数据。",
      "must_include": ["有边界调查型", "完成证据", "停止与升级条件", "Residual risks"],
      "must_exclude": ["保证找到根因", "审核结果:"]
    },
    {
      "id": "labeled-low-risk-assumption-zh",
      "mode": "discovery",
      "language": "zh",
      "input": "目标是给内部工具补充导出功能。默认使用现有测试框架这种低风险细节你可以决定，但必须说明你的假设。",
      "must_include": ["暂定假设", "现有测试框架"],
      "must_exclude": ["用户已确认使用现有测试框架", "综合评分:"]
    },
    {
      "id": "source-conflict-zh",
      "mode": "discovery",
      "language": "zh",
      "input": "API 文档说订单状态叫 paid，集成测试却断言 completed。请继续生成 Goal，但我还没决定以哪个为准。",
      "must_include": ["paid", "completed", "待确认"],
      "must_exclude": ["已决定以 API 文档为准", "已决定以集成测试为准", "审核结果:"]
    },
    {
      "id": "oversized-goal-zh",
      "mode": "discovery",
      "language": "zh",
      "input": "Goal 是重做整个 SaaS，包括认证、计费、CRM、数据仓库和移动端。",
      "must_include": ["拆分", "待确认"],
      "must_exclude": ["Goal 类型：", "审核结果:"]
    },
    {
      "id": "existing-draft-gap-zh",
      "mode": "discovery",
      "language": "zh",
      "input": "已有草稿：让报表 p95 小于 30 秒；范围是 reports；不改数据库；用 benchmark 验证。请只处理关键缺口。",
      "must_include": ["已确认", "待确认"],
      "must_exclude": ["请重新描述你的目标", "综合评分:"]
    },
    {
      "id": "revision-complete-regeneration-zh",
      "mode": "revision",
      "language": "zh",
      "input": "当前 Goal：让 checkout 成功时进入收据页、拒付时显示安全错误；AC-1 用 checkout-success 集成测试验证；AC-2 尚无证据；只改 web/checkout 及测试；不改 API 和数据库；保持现有错误语义；质量优先于速度；API schema 高于现有测试，冲突时升级；两次尝试没有新证据、没有减少阻塞且没有合理替代路径时停止；高风险或后端修改前确认；完成报告列出全部剩余工作类别。Reviewer 返回修订: Revision Required。问题：AC-2 没有证据。影响：无法判断失败路径是否完成。修改要求：指定失败路径测试。我的决定：使用 checkout-decline 集成测试。现在重新生成完整 Goal。",
      "must_include": ["Goal 类型", "AC-2", "checkout-decline", "完成报告要求"],
      "must_exclude": ["Patch", "补丁", "G-01", "综合评分:"]
    },
    {
      "id": "rejected-redefine-zh",
      "mode": "redefinition",
      "language": "zh",
      "input": "Reviewer 返回驳回: Rejected，因为原文只有‘尽力让系统更好’，没有可用目标。请继续。",
      "must_include": ["重新定义", "待确认"],
      "must_exclude": ["润色后的 Goal", "通过: Approved", "综合评分:"]
    },
    {
      "id": "approved-unchanged-handoff-zh",
      "mode": "handoff",
      "language": "zh",
      "goal_text": "目标：现有邮箱登录在成功、无效凭据和网络失败时均产生可观察结果。",
      "input": "Reviewer 已返回通过: Approved。请处理该 Goal。",
      "must_include": ["$task-decompose", "目标：现有邮箱登录在成功、无效凭据和网络失败时均产生可观察结果。"],
      "must_exclude": ["改写后的 Goal", "新的验收标准", "综合评分:"]
    },
    {
      "id": "unreviewed-draft-zh",
      "mode": "draft",
      "language": "zh",
      "input": "跳过审核，直接生成完整草稿：把 README 中失效的 install-old 命令替换为 CLI --help 中的 install-new；AC-1 是新用户逐条执行 README 命令均成功，证据是终端退出码和输出；只改 README，不改代码；CLI --help 是唯一事实来源；文档准确性优先；两次尝试无新证据且无替代路径时停止；代码修改或发布前确认；完成报告列出全部剩余工作类别。",
      "must_include": ["Unreviewed Draft"],
      "must_exclude": ["通过: Approved", "已经适合执行", "自动交给 $task-decompose"]
    },
    {
      "id": "role-only-when-relevant-zh",
      "mode": "draft",
      "language": "zh",
      "input": "生成完整 Goal：优化 web/ 的雷达图可读性；AC-1 是五名目标用户中至少四名能正确说出最高和最低维度，证据是可用性记录；只能修改 web/ 和对应测试，后端需求写入 handoff，不得修改 backend/；保持数据含义和 API；用户理解优先于视觉装饰；现有产品术语表高于组件文案；两次尝试无新证据且无替代路径时停止，涉及后端或数据语义时确认；以前端体验视角评估用户是否读得懂；完成报告列出全部剩余工作类别。",
      "must_include": ["前端体验", "web/", "backend/", "handoff"],
      "must_exclude": ["后端负责人", "审核结果:"]
    },
    {
      "id": "maker-never-judges-en",
      "mode": "draft",
      "language": "en",
      "input": "Create a complete Goal draft for a four-hour read-only accessibility audit of the login and checkout pages. Inspect keyboard navigation, labels, focus order, and contrast; deliver findings mapped to WCAG 2.2 AA with screenshots and reproduction steps. Do not change code or data. WCAG 2.2 is authoritative; escalate conflicts with local guidance. Evidence quality outranks finding count. Stop at four hours or after two attempts with no new evidence, no reduced blocker, and no reasonable alternative. Request confirmation before sensitive access or any write. Require all remaining-work categories. Do not review or score it.",
      "must_include": ["Goal type", "Acceptance criteria", "Evidence", "Stop and escalation"],
      "must_exclude": ["Review result:", "Overall score:", "通过: Approved", "修订: Revision Required", "驳回: Rejected"]
    },
    {
      "id": "flexible-compact-goal-zh",
      "mode": "draft",
      "language": "zh",
      "input": "生成简洁但完整的 Goal：把 README 中已失效的安装命令改成当前可运行命令；只改 README；以当前 CLI --help 为准；用文档命令逐条执行结果验收；不涉及代码。",
      "must_include": ["Goal 类型", "验收标准", "完成证据", "范围与权限", "事实来源", "停止与升级条件"],
      "must_exclude": ["审核结果:", "G-01", "补丁式修订"]
    }
  ]
}
```

- [ ] **Step 2: Write the static contract checker before the Skill exists**

Create `tmp_py/tmp_20260715_goal_make_contract.py`:

```python
"""Check the Snap Goal Make contract and persistent evaluation cases."""

from __future__ import annotations

import json
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
SKILL = ROOT / "skills" / "snap-goal-make" / "SKILL.md"
TEMPLATE = ROOT / "skills" / "snap-goal-make" / "references" / "goal-template.md"
CASES = ROOT / "tests" / "goal-make-cases.json"


def require(text: str, fragments: list[str], source: str) -> list[str]:
    """Return missing required fragments for one contract source."""
    return [f"{source}: missing {item!r}" for item in fragments if item not in text]


def main() -> int:
    """Validate Maker protocol, template, and evaluation coverage."""
    failures: list[str] = []
    if not SKILL.exists():
        failures.append("skill: missing skills/snap-goal-make/SKILL.md")
        skill_text = ""
    else:
        skill_text = SKILL.read_text(encoding="utf-8")
    if not TEMPLATE.exists():
        failures.append("template: missing references/goal-template.md")
        template_text = ""
    else:
        template_text = TEMPLATE.read_text(encoding="utf-8")

    failures.extend(require(skill_text, [
        "name: snap-goal-make",
        "Progressive Summary",
        "Authorized Context",
        "Outcome Goal",
        "Bounded Investigation Goal",
        "$snap-goal-review",
        "$task-decompose",
        "Unreviewed Draft",
        "complete Goal",
        "Do not score",
    ], "snap-goal-make/SKILL.md"))
    failures.extend(require(template_text, [
        "Goal Type",
        "Background and Value",
        "Acceptance Criteria",
        "Completion Evidence",
        "Scope and Authority",
        "Constraints and Priorities",
        "Sources of Truth",
        "Assumptions and Unknowns",
        "Stop and Escalation Conditions",
        "Completion Report Requirements",
        "Unfinished",
        "Unverifiable",
        "Blocked",
        "Out of scope",
        "Residual risks",
    ], "goal-template.md"))

    payload = json.loads(CASES.read_text(encoding="utf-8"))
    cases = payload["cases"]
    expected_ids = {
        "discovery-vague-outcome-zh",
        "context-aware-software-zh",
        "bounded-investigation-draft-zh",
        "labeled-low-risk-assumption-zh",
        "source-conflict-zh",
        "oversized-goal-zh",
        "existing-draft-gap-zh",
        "revision-complete-regeneration-zh",
        "rejected-redefine-zh",
        "approved-unchanged-handoff-zh",
        "unreviewed-draft-zh",
        "role-only-when-relevant-zh",
        "maker-never-judges-en",
        "flexible-compact-goal-zh",
    }
    observed_ids = {str(case["id"]) for case in cases}
    if observed_ids != expected_ids:
        failures.append(f"cases: ids differ: {sorted(observed_ids ^ expected_ids)}")
    for case in cases:
        for key in ("id", "mode", "language", "input", "must_include", "must_exclude"):
            if key not in case:
                failures.append(f"cases: {case.get('id', '<unknown>')} missing {key}")

    if failures:
        print("GOAL MAKE CONTRACT FAIL")
        for failure in failures:
            print(f"- {failure}")
        return 1
    print(f"GOAL MAKE CONTRACT PASS: {len(cases)} evaluation cases")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 3: Create and verify the project virtual environment**

Run these as separate commands:

```bash
/Users/kaka/.pyenv/versions/3.13.9/bin/python3 -m venv venv
source ./venv/bin/activate
which python3
which pip3
```

Expected: both `which` outputs start with `/Users/kaka/kDevelop/Skills/SnapTask/venv/`.

- [ ] **Step 4: Run the contract checker and verify RED**

After activating and verifying the venv again, run:

```bash
python3 tmp_py/tmp_20260715_goal_make_contract.py
```

Expected: exit 1 with `GOAL MAKE CONTRACT FAIL`, including missing `skills/snap-goal-make/SKILL.md` and `references/goal-template.md`.

- [ ] **Step 5: Implement the canonical Goal template**

Create `skills/snap-goal-make/references/goal-template.md` with these exact sections and rules:

````markdown
# Goal Template

Use every section. Keep simple Goals compact. When a field does not apply, state why instead of omitting it.

Respond in the user's language. For Chinese output, localize the canonical headings exactly:

| English | Chinese |
| --- | --- |
| Goal Type | Goal 类型 |
| Background and Value | 背景与价值 |
| Goal | 目标 |
| Acceptance Criteria | 验收标准 |
| Completion Evidence | 完成证据 |
| Scope and Authority | 范围与权限 |
| Constraints and Priorities | 约束与优先级 |
| Sources of Truth | 事实来源 |
| Assumptions and Unknowns | 假设与未知项 |
| Stop and Escalation Conditions | 停止与升级条件 |
| Completion Report Requirements | 完成报告要求 |

## Outcome Goal

```text
Goal Type:
Outcome Goal

Background and Value:
<current state, affected actor or system, and value of completion>

Goal:
Move <target> from <current state> to <observable final state>.

Acceptance Criteria:
- AC-1: Under <condition>, <object> produces <observable result>.

Completion Evidence:
| Acceptance ID | Required evidence | Verification method |
| AC-1 | <artifact or observation> | <exact check> |

Scope and Authority:
- Included: <required work>
- Excluded: <non-goals>
- Allowed: <read or change boundary>
- Prohibited: <forbidden action>

Constraints and Priorities:
- Preserve: <invariant>
- Limits: <time, cost, calls, or not applicable with reason>
- Trade-off priority: <ordered priorities>

Sources of Truth:
- Authority: <code, document, API, or data>
- Conflict rule: <priority or not applicable with reason>

Assumptions and Unknowns:
- Assumptions: <explicit assumptions or none>
- Unknowns: <unknowns or none>

Stop and Escalation Conditions:
- Complete when every acceptance criterion has its required evidence.
- Stop after <N> attempts with no new evidence, no reduced blocker, and no reasonable alternative.
- Request confirmation before <risky, irreversible, external-impact, costly, or unauthorized action>.

Completion Report Requirements:
- Completed and evidence
- Unfinished
- Unverifiable
- Blocked
- Out of scope
- Residual risks
```

## Bounded Investigation Goal

```text
Goal Type:
Bounded Investigation Goal

Background and Value:
<observed issue and why the investigation matters>

Goal:
Within <time, cost, or attempt limit>, inspect <explicit boundary> and deliver <verified facts, excluded hypotheses, evidence-supported likely explanations, unknowns, and next actions>. Discovery of a root cause is not guaranteed.

Acceptance Criteria:
- AC-1: Every named source or path is inspected and recorded.
- AC-2: Every conclusion identifies its evidence and confidence.
- AC-3: Required deliverables distinguish facts, hypotheses, unknowns, and next actions.

Completion Evidence:
| Acceptance ID | Required evidence | Verification method |
| AC-1 | <inspection record> | <source, identifier, time, check, finding> |
| AC-2 | <evidence map> | <cross-check each conclusion> |
| AC-3 | <final investigation report> | <section completeness check> |

Scope and Authority:
- Included: <sources, systems, and time range>
- Excluded: <non-goals>
- Allowed: <normally read-only boundary>
- Prohibited: <writes or sensitive access not authorized>

Constraints and Priorities:
- Preserve: <safety, privacy, and production invariants>
- Limits: <time, cost, or attempt count>
- Trade-off priority: evidence quality and safety over explanation count or speed, unless the user decides otherwise.

Sources of Truth:
- Authority: <ordered sources>
- Conflict rule: <record and escalate unresolved conflict>

Assumptions and Unknowns:
- Assumptions: <explicit assumptions or none>
- Unknowns: <unknowns or none>

Stop and Escalation Conditions:
- Complete when the inquiry boundary and deliverables are exhausted, even if root cause remains unknown.
- Stop when the limit is reached or after <N> attempts with no new evidence, no reduced blocker, and no reasonable alternative.
- Request confirmation before sensitive access, writes, external impact, destructive action, or expansion beyond scope.

Completion Report Requirements:
- Completed inspections and evidence
- Unfinished
- Unverifiable
- Blocked
- Out of scope
- Residual risks
```

## Role Perspective

Include a role perspective only when it defines an allowed boundary, trade-off criterion, or review lens. Never add a ceremonial title. Put the resulting restriction in `Scope and Authority` or `Constraints and Priorities`.
````

- [ ] **Step 6: Implement the Maker Skill**

Create `skills/snap-goal-make/SKILL.md`:

````markdown
---
name: snap-goal-make
description: Use when a rough intention, project objective, investigation idea, or existing Goal draft must be turned into a complete AI-agent Goal through context-aware multi-turn conversation before independent review.
---

# Snap Goal Make

## Overview

Turn rough intent into a complete Goal through progressive conversation. Support general AI-agent work and read authorized project rules, code, interfaces, tests, and documentation when the Goal concerns software.

## Core Boundary

Create and revise Goals; do not judge their quality. `$snap-goal-review` is the sole quality judge.

- Do not score the Goal or apply the Reviewer's hard gates as an internal review.
- Do not emit `通过: Approved`, `修订: Revision Required`, or `驳回: Rejected` as your own verdict.
- Do not invent requirements, silently resolve source conflicts, or turn a recommendation into a confirmed user decision.
- Do not use patch-based revision, Goal version numbers, or hidden persistent workflow state.
- After Review returns Approved, preserve the complete Goal unchanged.

## Entry Selection

Choose the entry from the user's current input:

- Rough intent: start with Intake and Discovery.
- Existing Goal draft: preserve usable content and ask only about decisions that materially affect the Goal.
- Review report: follow Result Handling.
- Approved Goal: hand the unchanged Goal to `$task-decompose` with the review result and evidence expectations.

If the request contains several independent initiatives, propose Goal boundaries first and ask the user to choose one. Do not draft one Goal that hides several independent outcomes.

## Authorized Context

1. Identify what sources the user supplied or authorized.
2. For software work, read nearby `AGENTS.md`, relevant code, interfaces, tests, and project documentation before asking questions those files can answer.
3. Treat discovered facts as context, not as user preference.
4. When sources conflict, state each source and conflict, then ask for a priority or escalation rule.
5. When access fails, name the unavailable source and continue only with explicit unknowns.
6. Do not inspect unrelated files merely to fill every template field.

## Goal Type

Choose one drafting form from `references/goal-template.md`:

- Outcome Goal: an observable final state can be defined.
- Bounded Investigation Goal: discovery cannot be guaranteed, but inquiry boundary, deliverables, evidence, limits, and stopping point can be defined.

Never rewrite an uncertain investigation as a promise to find a root cause.

## Multi-Turn Protocol

### Intake

Restate the intended change or inquiry in one sentence. Identify authorized sources and whether the request is one Goal or several independent Goals.

### Discovery

Resolve one primary decision per turn. Ask only when the answer changes the outcome, acceptance, evidence, scope, authority, invariant, priority, source rule, or stop condition.

- Read authorized context instead of asking the user for discoverable facts.
- Ask the user about consequential preferences and decisions.
- Make a low-risk assumption only when it does not change a critical decision; label it explicitly.
- When the user asks you to decide a consequential preference, propose a recommendation and wait for confirmation.

### Progressive Summary

During Discovery, keep the response compact:

```text
已确认 / Confirmed:
- <confirmed facts and decisions>

暂定假设 / Tentative assumptions:
- <labeled low-risk assumption or none>

待确认 / Open decision:
- <the one decision that blocks useful progress>
```

End with one primary question. Do not print the complete Goal on every turn.

### Draft

When the consequential decisions are resolved, generate one complete Goal using every heading in `references/goal-template.md`. Keep the length proportional to risk and complexity. Explain why a field is not applicable instead of omitting it.

- Respond in the user's language and apply the exact localized heading map from the template.
- Number acceptance criteria `AC-1`, `AC-2`, and so on.
- Map every acceptance ID to concrete evidence and a verification method.
- Include source priority or explain why conflict handling is not applicable.
- Include a precise no-progress rule and human escalation triggers.
- Require the completion report to expose Completed, Unfinished, Unverifiable, Blocked, Out of scope, and Residual risks.
- Include a role perspective only when it constrains authority, trade-offs, or review criteria.

Label the output `Goal Draft` and ask the user to confirm it before review. If the user explicitly skips review, label it `Unreviewed Draft`; do not claim it is execution-ready and do not proactively hand it to task decomposition. An explicit bypass is handled by `$task-decompose` under its own rules.

## Review Handoff

After user confirmation, hand the complete Goal unchanged to `$snap-goal-review`. The handoff contains only:

1. The confirmed complete Goal.
2. Explicitly authorized sources needed for review.
3. Explicit user decisions that are not already legible in the Goal.

Do not include persuasion, desired score, or a requested verdict. If `$snap-goal-review` is unavailable, return the complete Goal plus an exact prompt telling the user to review it with `$snap-goal-review`; never simulate the verdict.

## Result Handling

Consume the Reviewer's existing result without changing its meaning:

- `通过: Approved`: preserve the complete Goal unchanged and hand it, the review result, and evidence expectations to `$task-decompose`.
- `修订: Revision Required`: use each finding's Problem, Impact, and Revision requirement to ask only for affected decisions. Then regenerate the complete Goal, show it in full, and request user confirmation before another complete review.
- `驳回: Rejected`: preserve valid background facts, return to defining the outcome or bounded inquiry, and do not cosmetically rewrite the rejected statement.

If the same unresolved issue returns twice without new information, pause and ask the user for the missing decision. Never lower acceptance, evidence, boundaries, safety, or stop rules merely to obtain approval.

## Output Shapes

### Discovery

```text
目标理解 / Goal understanding:
<one sentence>

已确认 / Confirmed:
- ...

暂定假设 / Tentative assumptions:
- ...

待确认 / Open decision:
- ...

问题 / Question:
<one primary question>
```

### Goal Draft

Use the complete selected form from `references/goal-template.md`, then ask for confirmation. Do not append a score or review verdict.

### Revision

```text
本轮处理的 Reviewer 反馈:
- <problem and required decision>

<the complete regenerated Goal>

请确认这份完整 Goal 是否可以重新交给 $snap-goal-review。
```

### Approved Handoff

```text
<the unchanged approved Goal>

审核结果与证据要求:
<the Reviewer result>

下一步:
- 交给 $task-decompose。
```

## Common Mistakes

- Rebuilding the Reviewer inside Maker.
- Dumping the full template before consequential decisions are known.
- Asking for facts available in authorized local files.
- Treating current implementation as the user's desired final state.
- Hiding an unresolved source conflict inside an assumption.
- Editing only one field after review instead of regenerating the complete Goal.
- Adding a role title that changes no boundary or evaluation criterion.
````

- [ ] **Step 7: Run static validation and verify GREEN**

Activate and verify the venv, then run:

```bash
python3 tmp_py/tmp_20260715_goal_make_contract.py
python3 /Users/kaka/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/snap-goal-make
```

Expected:

```text
GOAL MAKE CONTRACT PASS: 14 evaluation cases
Skill is valid!
```

- [ ] **Step 8: Commit the core Skill and contract**

```bash
git add skills/snap-goal-make/SKILL.md skills/snap-goal-make/references/goal-template.md tests/goal-make-cases.json tmp_py/tmp_20260715_goal_make_contract.py
git commit -m "Add multi-turn Goal maker"
```

Expected: one commit containing only the four listed files.

---

### Task 2: Wire Reviewer, launcher metadata, plugin metadata, and repository guidance

**Files:**
- Create: `skills/snap-goal-make/agents/openai.yaml`
- Modify: `skills/snap-goal-review/SKILL.md:29-34`
- Modify: `skills/task-decompose/SKILL.md:15-21`
- Modify: `.codex-plugin/plugin.json`
- Modify: `AGENTS.md`
- Modify: `tmp_py/tmp_20260715_goal_make_contract.py`

**Interfaces:**
- Consumes: the exact Review result contract already produced by `snap-goal-review`.
- Produces: discoverable `$snap-goal-make`, corrected revision handoff, and plugin-level create-review-decompose prompts.

- [ ] **Step 1: Extend the contract checker with failing integration checks**

Add these constants after `CASES`:

```python
AGENT = ROOT / "skills" / "snap-goal-make" / "agents" / "openai.yaml"
REVIEW_SKILL = ROOT / "skills" / "snap-goal-review" / "SKILL.md"
TASK_SKILL = ROOT / "skills" / "task-decompose" / "SKILL.md"
PLUGIN = ROOT / ".codex-plugin" / "plugin.json"
GUIDE = ROOT / "AGENTS.md"
```

Before `if failures:`, add:

```python
    agent_text = AGENT.read_text(encoding="utf-8") if AGENT.exists() else ""
    review_text = REVIEW_SKILL.read_text(encoding="utf-8")
    task_text = TASK_SKILL.read_text(encoding="utf-8")
    plugin = json.loads(PLUGIN.read_text(encoding="utf-8"))
    guide_text = GUIDE.read_text(encoding="utf-8")
    failures.extend(require(agent_text, [
        'display_name: "Snap Goal Make"',
        "$snap-goal-make",
    ], "snap-goal-make/agents/openai.yaml"))
    failures.extend(require(review_text, [
        "return findings to `$snap-goal-make`",
    ], "snap-goal-review/SKILL.md"))
    if "`$snap-goal`" in review_text:
        failures.append("snap-goal-review/SKILL.md: obsolete $snap-goal handoff remains")
    failures.extend(require(task_text, [
        "return the Goal to `$snap-goal-make`",
    ], "task-decompose/SKILL.md"))
    if "`$snap-goal`" in task_text:
        failures.append("task-decompose/SKILL.md: obsolete $snap-goal handoff remains")
    if plugin.get("version") != "0.2.0":
        failures.append(f"plugin: expected version 0.2.0, got {plugin.get('version')!r}")
    plugin_text = json.dumps(plugin, ensure_ascii=False)
    failures.extend(require(plugin_text, [
        "Goal creation",
        "Create a Goal through guided conversation.",
    ], ".codex-plugin/plugin.json"))
    failures.extend(require(guide_text, [
        "skills/snap-goal-make/",
        "goal-make-cases.json",
    ], "AGENTS.md"))
```

Activate and verify the venv, then run:

```bash
python3 tmp_py/tmp_20260715_goal_make_contract.py
```

Expected: `GOAL MAKE CONTRACT FAIL` for missing agent metadata, old Reviewer handoff, plugin version/description, and repository guidance.

- [ ] **Step 2: Add Maker launcher metadata**

Create `skills/snap-goal-make/agents/openai.yaml`:

```yaml
interface:
  display_name: "Snap Goal Make"
  short_description: "Create complete Goals through guided conversation"
  default_prompt: "Use $snap-goal-make to turn this intent into a complete Goal through multi-turn conversation, then hand the confirmed Goal to $snap-goal-review."
```

- [ ] **Step 3: Correct both revision handoffs**

In `skills/snap-goal-review/SKILL.md`, replace only this line:

```markdown
- `修订: Revision Required`: return findings to `$snap-goal-make` when available, or to the Goal author. Do not produce final task cards.
```

Do not change the Reviewer's rubric, score, verdict labels, or output sections.

In `skills/task-decompose/SKILL.md`, replace only the Revision Required line with:

```markdown
- `修订: Revision Required`: use Discovery Mode only to clarify the listed gaps; return the Goal to `$snap-goal-make` or its author before final cards.
```

Do not change the Approved, Rejected, unreviewed bypass, or decomposition behavior.

- [ ] **Step 4: Update plugin metadata**

Replace `.codex-plugin/plugin.json` with this complete manifest:

```json
{
  "name": "snaptask",
  "version": "0.2.0",
  "description": "Composable Codex skills for guided AI-agent Goal creation, independent Goal review, and decomposition into verifiable task cards.",
  "author": {
    "name": "KakaTelnet",
    "url": "https://github.com/KakaTelnet"
  },
  "homepage": "https://github.com/KakaTelnet/SnapTask",
  "repository": "https://github.com/KakaTelnet/SnapTask",
  "keywords": [
    "ai-agent",
    "goals",
    "goal-creation",
    "goal-review",
    "task-decomposition",
    "codex-skills"
  ],
  "skills": "./skills/",
  "interface": {
    "displayName": "SnapTask",
    "shortDescription": "Create, review, and decompose AI-agent Goals",
    "longDescription": "SnapTask guides users from rough intent to a complete AI-agent Goal, reviews whether that Goal is verifiable, bounded, safe, and complete, then decomposes approved work into low-conflict task cards.",
    "developerName": "KakaTelnet",
    "category": "Developer Tools",
    "capabilities": [
      "Interactive",
      "Read",
      "Write"
    ],
    "defaultPrompt": [
      "Create a Goal through guided conversation.",
      "Review this Goal before I decompose it.",
      "Turn this approved Goal into AI-ready task cards."
    ],
    "websiteURL": "https://github.com/KakaTelnet/SnapTask"
  }
}
```

- [ ] **Step 5: Update repository guidance**

In `AGENTS.md`:

1. Add `skills/snap-goal-make/` as the guided Goal generator before the Reviewer entry.
2. Add these read commands under validation:

```text
- `sed -n '1,260p' skills/snap-goal-make/SKILL.md` reviews the multi-turn Goal creation workflow.
- `sed -n '1,260p' skills/snap-goal-make/references/goal-template.md` reviews the canonical Goal forms.
- `python3 tmp_py/tmp_20260715_goal_make_contract.py` checks the Maker contract and fixture coverage.
```

3. Add these manual checks under Testing Guidelines:

```text
- Goal Maker never scores or emits a review verdict as its own judgment.
- Confirmed Goal drafts hand off to `$snap-goal-review`; approved Goals hand off unchanged to `$task-decompose`.
- `tests/goal-make-cases.json` retains all required Discovery, Draft, Revision, Rejected, Approved, and bypass scenarios.
```

- [ ] **Step 6: Run integration validation and verify GREEN**

Activate and verify the venv, then run:

```bash
python3 tmp_py/tmp_20260715_goal_make_contract.py
python3 /Users/kaka/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/snap-goal-make
python3 /Users/kaka/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/snap-goal-review
python3 /Users/kaka/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/task-decompose
python3 /Users/kaka/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py .
```

Expected: contract pass, three `Skill is valid!` lines, and `Plugin validation passed`.

Also run:

```bash
jq empty .codex-plugin/plugin.json tests/goal-make-cases.json tests/goal-review-cases.json
git diff --check
```

Expected: both commands exit 0 with no output.

- [ ] **Step 7: Commit the integration wiring**

```bash
git add skills/snap-goal-make/agents/openai.yaml skills/snap-goal-review/SKILL.md skills/task-decompose/SKILL.md .codex-plugin/plugin.json AGENTS.md tmp_py/tmp_20260715_goal_make_contract.py
git commit -m "Wire Goal maker into SnapTask"
```

Expected: one commit containing only the six listed paths.

---

### Task 3: Add fresh-context behavioral evaluation

**Files:**
- Create: `tmp_py/tmp_20260715_run_goal_make_evals.py`
- Read: `tests/goal-make-cases.json`
- Read: `skills/snap-goal-make/SKILL.md`

**Interfaces:**
- Consumes: one evaluation case and a clean Codex CLI execution context.
- Produces: one response file per case plus a pass/fail summary based on required and forbidden text.

- [ ] **Step 1: Create the behavior harness**

Create `tmp_py/tmp_20260715_run_goal_make_evals.py`:

```python
"""Run fresh-context behavioral evaluations for snap-goal-make."""

from __future__ import annotations

import argparse
import json
import shutil
import subprocess
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
CASES_PATH = ROOT / "tests" / "goal-make-cases.json"
OUTPUT_DIR = Path("/private/tmp/snap-goal-make-evals")


def parse_args() -> argparse.Namespace:
    """Parse optional case selection and repeat count."""
    parser = argparse.ArgumentParser()
    parser.add_argument("--ids", help="Comma-separated case ids; default is all cases")
    parser.add_argument("--repeat", type=int, default=1)
    return parser.parse_args()


def run_case(codex: str, case: dict[str, object], run_number: int) -> dict[str, object]:
    """Run one isolated Maker turn and validate required and forbidden markers."""
    case_id = str(case["id"])
    output_path = OUTPUT_DIR / f"{case_id}-{run_number}.txt"
    goal_text = str(case.get("goal_text", ""))
    prompt = (
        "First read and follow skills/snap-goal-make/SKILL.md and every reference it "
        "requires. Read only repository files authorized by the user input. Treat the "
        "following as the current turn of a Goal-making conversation. Do not perform "
        "independent Goal review.\n\n"
        f"Expected conversation entry: {case['mode']}\n"
        f"Response language: {case['language']}\n"
        f"Current confirmed Goal when supplied:\n{goal_text}\n\n"
        f"User input:\n{case['input']}"
    )
    command = [
        codex,
        "exec",
        "--ephemeral",
        "--ignore-user-config",
        "--sandbox",
        "read-only",
        "--color",
        "never",
        "--cd",
        str(ROOT),
        "--output-last-message",
        str(output_path),
        prompt,
    ]
    completed = subprocess.run(
        command,
        cwd=ROOT,
        capture_output=True,
        text=True,
        timeout=300,
        check=False,
    )
    response = output_path.read_text(encoding="utf-8") if output_path.exists() else ""
    response_folded = response.casefold()
    failures: list[str] = []
    if completed.returncode != 0:
        failures.append(f"codex exit {completed.returncode}")
    for marker in case["must_include"]:
        if str(marker).casefold() not in response_folded:
            failures.append(f"missing required marker {marker!r}")
    for marker in case["must_exclude"]:
        if str(marker).casefold() in response_folded:
            failures.append(f"found forbidden marker {marker!r}")
    return {
        "id": case_id,
        "run": run_number,
        "passed": not failures,
        "failures": failures,
        "output": str(output_path),
    }


def main() -> int:
    """Run selected evaluations and print a compact summary."""
    args = parse_args()
    codex = shutil.which("codex")
    if codex is None:
        raise SystemExit("codex executable not found")
    payload = json.loads(CASES_PATH.read_text(encoding="utf-8"))
    cases = payload["cases"]
    if args.ids:
        selected = set(args.ids.split(","))
        cases = [case for case in cases if case["id"] in selected]
        missing = selected - {case["id"] for case in cases}
        if missing:
            raise SystemExit(f"unknown case ids: {sorted(missing)}")
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    results = [
        run_case(codex, case, run_number)
        for case in cases
        for run_number in range(1, args.repeat + 1)
    ]
    for result in results:
        marker = "PASS" if result["passed"] else "FAIL"
        print(f"{marker} {result['id']}#{result['run']}: {result['output']}")
        for failure in result["failures"]:
            print(f"  - {failure}")
    failed = sum(not result["passed"] for result in results)
    print(f"EVAL SUMMARY: {len(results) - failed}/{len(results)} passed")
    return 1 if failed else 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: Run representative behavior cases**

Activate and verify the venv, then run:

```bash
python3 tmp_py/tmp_20260715_run_goal_make_evals.py --ids discovery-vague-outcome-zh,bounded-investigation-draft-zh,revision-complete-regeneration-zh,approved-unchanged-handoff-zh,maker-never-judges-en
```

Expected: `EVAL SUMMARY: 5/5 passed`.

If a case fails, inspect its exact output under `/private/tmp/snap-goal-make-evals/`. Change only the narrow Maker instruction responsible for the mismatch, rerun the failing case, then rerun the five-case set. Do not weaken `must_include` or `must_exclude` merely to make a failure disappear.

- [ ] **Step 3: Run all behavior cases**

Activate and verify the venv, then run:

```bash
python3 tmp_py/tmp_20260715_run_goal_make_evals.py
```

Expected: `EVAL SUMMARY: 14/14 passed`.

- [ ] **Step 4: Run stability repetitions for the three highest-risk boundaries**

Activate and verify the venv, then run:

```bash
python3 tmp_py/tmp_20260715_run_goal_make_evals.py --ids maker-never-judges-en,revision-complete-regeneration-zh,approved-unchanged-handoff-zh --repeat 3
```

Expected: `EVAL SUMMARY: 9/9 passed`, with no Maker-generated verdict, no patch/version output, and no modification of the approved Goal.

- [ ] **Step 5: Commit the behavior harness and any narrow protocol hardening**

```bash
git add tmp_py/tmp_20260715_run_goal_make_evals.py skills/snap-goal-make/SKILL.md tests/goal-make-cases.json
git commit -m "Add Goal maker behavior evaluations"
```

Before committing, use `git diff --cached --name-only` to confirm only paths intentionally changed by this task are staged. If the Skill and cases required no hardening, commit only the harness.

---

### Task 4: Validate isolated plugin discovery and end-to-end handoff

**Files:**
- Read: `.codex-plugin/plugin.json`
- Read: all three `skills/*/SKILL.md`
- Do not modify source files unless a validator or end-to-end check exposes a real contract defect.

**Interfaces:**
- Produces: evidence that a clean Codex home discovers and triggers `$snap-goal-make`, `$snap-goal-review`, and `$task-decompose` from the packaged plugin.

- [ ] **Step 1: Run final repository validation**

Activate and verify the venv, then run:

```bash
python3 tmp_py/tmp_20260715_goal_make_contract.py
python3 tmp_py/tmp_20260715_goal_review_contract.py
python3 /Users/kaka/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/snap-goal-make
python3 /Users/kaka/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/snap-goal-review
python3 /Users/kaka/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/task-decompose
python3 /Users/kaka/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py .
```

Expected: both contracts pass, all three Skills are valid, and plugin validation passes.

Run non-Python checks:

```bash
jq empty .codex-plugin/plugin.json tests/goal-make-cases.json tests/goal-review-cases.json
git diff --check
git status --short --branch
```

Expected: JSON and diff checks exit 0. Git status may still show the three pre-existing untracked user work areas, but no unintended file is staged.

- [ ] **Step 2: Create an isolated local marketplace fixture**

Use a temporary `CODEX_HOME=/private/tmp/snaptask-make-codex-home`; never edit the user's permanent marketplace or Codex config. Create the fixture directories:

```bash
mkdir -p /private/tmp/snaptask-make-marketplace/.agents/plugins
mkdir -p /private/tmp/snaptask-make-marketplace/plugins
ln -s /Users/kaka/kDevelop/Skills/SnapTask /private/tmp/snaptask-make-marketplace/plugins/snaptask
```

Create `/private/tmp/snaptask-make-marketplace/.agents/plugins/marketplace.json` with:

```json
{
  "name": "snaptask-make-test",
  "interface": {
    "displayName": "SnapTask Make Test"
  },
  "plugins": [
    {
      "name": "snaptask",
      "source": {
        "source": "local",
        "path": "./plugins/snaptask"
      },
      "policy": {
        "installation": "AVAILABLE",
        "authentication": "ON_INSTALL"
      },
      "category": "Developer Tools"
    }
  ]
}
```

Validate and install:

```bash
jq empty /private/tmp/snaptask-make-marketplace/.agents/plugins/marketplace.json
env CODEX_HOME=/private/tmp/snaptask-make-codex-home codex plugin marketplace add /private/tmp/snaptask-make-marketplace --json
env CODEX_HOME=/private/tmp/snaptask-make-codex-home codex plugin add snaptask@snaptask-make-test --json
env CODEX_HOME=/private/tmp/snaptask-make-codex-home codex plugin list --json
```

Expected: `snaptask` version `0.2.0` is installed and enabled from marketplace `snaptask-make-test`.

- [ ] **Step 3: Verify all three installed Skill triggers**

Using the isolated `CODEX_HOME`, run:

```bash
env CODEX_HOME=/private/tmp/snaptask-make-codex-home codex exec --ephemeral --sandbox read-only --cd /Users/kaka/kDevelop/Skills/SnapTask --output-last-message /private/tmp/snap-goal-make-trigger.txt "Use \$snap-goal-make. I want to make login better. Start the Goal-making conversation; do not review or score."
```

Expected: the output names the installed `snaptask:snap-goal-make` Skill, contains Confirmed/Tentative assumptions/Open decision semantics and one primary question, and contains no score or verdict.

```bash
env CODEX_HOME=/private/tmp/snaptask-make-codex-home codex exec --ephemeral --sandbox read-only --cd /Users/kaka/kDevelop/Skills/SnapTask --output-last-message /private/tmp/snap-goal-review-trigger.txt "Use \$snap-goal-review to review only this Goal and do not rewrite it: Try to make the whole system better."
```

Expected: the output names `snaptask:snap-goal-review`, starts with `驳回: Rejected`, contains the full existing review contract, and does not rewrite the Goal.

```bash
env CODEX_HOME=/private/tmp/snaptask-make-codex-home codex exec --ephemeral --sandbox read-only --cd /Users/kaka/kDevelop/Skills/SnapTask --output-last-message /private/tmp/task-decompose-trigger.txt "Use \$task-decompose. State how you handle Goal review results Approved, Revision Required, and Rejected, including the revision destination. Do not create task cards."
```

Expected: the output names `snaptask:task-decompose`, preserves all three handoff rules, and names `$snap-goal-make` as the revision destination.

Expected: each invocation reports the installed plugin-qualified Skill name and follows the installed file, not an ambient or cached Skill.

- [ ] **Step 4: Review the final diff**

Run:

```bash
git diff origin/main...HEAD --stat
git diff origin/main...HEAD -- .codex-plugin/plugin.json AGENTS.md skills/snap-goal-make skills/snap-goal-review/SKILL.md skills/task-decompose/SKILL.md tests/goal-make-cases.json tmp_py/tmp_20260715_goal_make_contract.py tmp_py/tmp_20260715_run_goal_make_evals.py
```

Confirm:

- no duplicate Reviewer logic exists in Maker;
- no Goal version or patch protocol exists;
- Reviewer and Task Decompose only change their obsolete revision destination;
- approved Goal handoff is unchanged;
- all fourteen Maker cases remain intact;
- pre-existing untracked user files remain unmodified and unstaged.

- [ ] **Step 5: Record completion state**

Run:

```bash
git log -4 --oneline
git status --short --branch
```

Report the implementation commits, exact validation results, isolated installation evidence, and any remaining untracked user files. Do not push unless the user explicitly requests it.
