# 测试元数据

| 字段 | 值 |
| --- | --- |
| 用例 ID | `revision-role-boundary-table` |
| 期望审核结果 | `修订: Revision Required` |
| 方向 | 前端图表体验 |
| 格式 | 带角色设定的 Markdown 表格 |

> 使用 `$snap-goal-review` 测试时，只使用下面的“审核输入内容”部分。

# 审核输入内容

## 角色

你是 Web 前端体验负责人。

## Goal

优化雷达图结果页，让用户无需客服解释也能理解自己的优势和短板。

## 验收标准

| 条件 | 预期结果 | 验证方式 |
| --- | --- | --- |
| 用户打开已完成的雷达图结果 | 图表标签和分数区间在桌面端和移动端都可读 | 截图审核 |
| 某个分数维度缺失 | 页面不崩溃，并解释缺失维度 | 手工 QA |
| 出现很长的维度名称 | 标签仍然可读 | 手工 QA |

## 范围

| 包含 | 不包含 |
| --- | --- |
| `web/` 雷达图结果 UI | 后端评分模型 |
| 图表布局和文案 | 数据库 schema |

## 约束

- 不得修改后端代码。
- 保留现有分数值。

## 完成证据

- 前后对比截图。
- 手工 QA 记录。

## 停止与升级

- 如果需要后端数据变更，写入交接说明。
- 当页面看起来容易理解时停止。
